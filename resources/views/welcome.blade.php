@extends('layouts.app')
@section('content')
<section class="rounded-3xl bg-gradient-to-br from-brand-700 to-brand-500 p-8 text-white"><p class="font-semibold">Yayasan Sosial Sumsel Peduli</p><h1 class="mt-3 max-w-3xl text-4xl font-bold">Platform donasi lokal transparan untuk masyarakat Sumatera Selatan.</h1><p class="mt-4 max-w-2xl text-emerald-50">Donasi manual via transfer bank, wajib upload bukti, dan diverifikasi admin sebelum dana masuk ke progress campaign.</p></section>
<div class="mt-8 grid gap-4 md:grid-cols-3"><x-stat-card label="Campaign Aktif" :value="$stats['campaigns']"/><x-stat-card label="Dana Terkumpul" :value="'Rp '.number_format($stats['collected'],0,',','.')"/><x-stat-card label="Program Lokal" :value="$stats['beneficiaries']"/></div>
<form class="mt-8 grid gap-3 rounded-2xl bg-white p-4 md:grid-cols-3"><input class="input" name="q" value="{{ request('q') }}" placeholder="Cari campaign"><select class="input" name="category"><option value="">Semua kategori</option>@foreach($categories as $category)<option value="{{ $category->slug }}" @selected(request('category')===$category->slug)>{{ $category->name }}</option>@endforeach</select><button class="btn-primary">Cari</button></form>
<div class="mt-8 grid gap-6 md:grid-cols-3">@foreach($campaigns as $campaign)<article class="card"><div class="h-36 rounded-xl bg-emerald-100"></div><p class="mt-4 text-xs font-semibold uppercase text-brand-700">{{ $campaign->category->name }}</p><h2 class="mt-1 text-lg font-bold">{{ $campaign->title }}</h2><p class="mt-2 text-sm text-slate-600">{{ $campaign->short_description }}</p><div class="mt-4 h-3 overflow-hidden rounded-full bg-slate-100"><div class="h-full bg-brand-600" style="width: {{ $campaign->progressPercentage() }}%"></div></div><p class="mt-2 text-sm font-semibold">Rp {{ number_format($campaign->collected_amount,0,',','.') }} / Rp {{ number_format($campaign->target_amount,0,',','.') }}</p><a class="btn-primary mt-4 w-full" href="{{ route('campaigns.show',$campaign->slug) }}">Detail & Donasi</a></article>@endforeach</div><div class="mt-8">{{ $campaigns->links() }}</div>
@endsection
