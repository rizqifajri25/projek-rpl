@extends('layouts.app')@section('content')<h1 class="text-3xl font-bold">Dashboard Donatur</h1><div class="mt-6 space-y-4">@foreach($donations as $donation)<div class="card flex flex-col justify-between gap-3 md:flex-row"><div><p class="font-bold">{{ $donation->campaign->title }}</p><p>Rp {{ number_format($donation->amount,0,',','.') }} · {{ $donation->status }}</p></div>@if($donation->certificate)<a class="btn-primary" href="{{ route('certificates.download',$donation->certificate) }}">Download Sertifikat</a>@endif</div>@endforeach</div>@endsection
