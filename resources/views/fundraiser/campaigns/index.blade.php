@extends('layouts.app')@section('content')<div class="flex justify-between"><h1 class="text-3xl font-bold">Campaign Saya</h1><a class="btn-primary" href="{{ route('fundraiser.campaigns.create') }}">Tambah</a></div><div class="mt-6 space-y-4">@foreach($campaigns as $campaign)<div class="card flex justify-between"><div><p class="font-bold">{{ $campaign->title }}</p><p class="text-sm">{{ $campaign->status }} · {{ $campaign->category->name }}</p></div><a class="btn-secondary" href="{{ route('fundraiser.campaigns.edit',$campaign) }}">Edit</a></div>@endforeach</div>{{ $campaigns->links() }}@endsection
