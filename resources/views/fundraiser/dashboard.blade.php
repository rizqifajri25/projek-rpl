@extends('layouts.app')@section('content')<div class="flex items-center justify-between"><h1 class="text-3xl font-bold">Dashboard Fundraiser</h1><a class="btn-primary" href="{{ route('fundraiser.campaigns.create') }}">Buat Campaign</a></div><div class="mt-6 grid gap-4 md:grid-cols-3">@foreach($campaigns as $campaign)<div class="card"><p class="badge bg-emerald-100 text-emerald-700">{{ $campaign->status }}</p><h2 class="mt-3 font-bold">{{ $campaign->title }}</h2><p class="text-sm text-slate-600">Rp {{ number_format($campaign->collected_amount,0,',','.') }} terkumpul</p><a class="mt-4 inline-block text-brand-700" href="{{ route('fundraiser.campaigns.edit',$campaign) }}">Kelola</a></div>@endforeach</div>@endsection
