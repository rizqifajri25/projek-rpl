<!doctype html><html lang="id"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1"><meta name="csrf-token" content="{{ csrf_token() }}"><title>{{ $title ?? 'Sumsel Peduli' }}</title>@vite(['resources/css/app.css','resources/js/app.js'])</head><body class="bg-slate-50 text-slate-800"><nav class="border-b bg-white"><div class="mx-auto flex max-w-7xl items-center justify-between px-4 py-4"><a href="{{ route('home') }}" class="text-xl font-bold text-brand-700">Sumsel Peduli</a><div class="flex items-center gap-4 text-sm"><a href="{{ route('campaigns.index') }}">Campaign</a>@auth<a href="{{ route('dashboard') }}">Dashboard</a><form method="post" action="{{ route('logout') }}">@csrf<button>Logout</button></form>@else<a href="{{ route('login') }}">Login</a><a class="btn-primary" href="{{ route('register') }}">Daftar</a>@endauth</div></div></nav>@if(session('success'))<div class="mx-auto mt-4 max-w-7xl rounded-xl bg-emerald-100 p-4 text-emerald-800">{{ session('success') }}</div>@endif<main class="mx-auto max-w-7xl px-4 py-8">{{ $slot ?? '' }}@yield('content')</main></body></html>
