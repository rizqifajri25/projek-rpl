@extends('layouts.app')@section('content')<h1 class="text-3xl font-bold">Verifikasi Campaign</h1><div class="mt-6 space-y-4">@foreach($campaigns as $campaign)<div class="card"><div class="flex flex-col justify-between gap-4 md:flex-row"><div><p class="font-bold">{{ $campaign->title }}</p><p class="text-sm text-slate-600">{{ $campaign->category->name }} · {{ $campaign->status }} · Rp {{ number_format($campaign->target_amount,0,',','.') }}</p><p class="text-sm">{{ $campaign->short_description }}</p></div><form method="post" action="{{ route('admin.campaigns.verify',$campaign) }}" class="flex flex-col gap-2 md:w-80">@csrf @method('patch')<textarea class="input" name="admin_notes" placeholder="Catatan verifikasi"></textarea><div class="flex gap-2"><button class="btn-primary flex-1" name="action" value="approve">Approve</button><button class="btn-secondary flex-1" name="action" value="reject">Reject</button></div></form></div></div>@endforeach</div><div class="mt-6">{{ $campaigns->links() }}</div>@endsection
