@extends('layouts.app')@section('content')<h1 class="text-3xl font-bold">Verifikasi Donasi Manual</h1><div class="mt-6 space-y-4">@foreach($donations as $donation)<div class="card"><div class="grid gap-4 md:grid-cols-3"><div><p class="font-bold">{{ $donation->donor_name }}</p><p class="text-sm text-slate-600">{{ $donation->campaign->title }}</p><p class="font-semibold text-brand-700">Rp {{ number_format($donation->amount,0,',','.') }}</p></div><div class="text-sm"><p>Bank: {{ $donation->proof->bank_name }}</p><p>Rekening: {{ $donation->proof->account_name }}</p><a class="text-brand-700" href="{{ Storage::url($donation->proof->file_path) }}" target="_blank">Lihat bukti transfer</a></div><form method="post" action="{{ route('admin.donations.verify',$donation) }}" class="space-y-2">@csrf @method('patch')<textarea class="input" name="admin_notes" placeholder="Catatan admin"></textarea><div class="flex gap-2"><button class="btn-primary flex-1" name="action" value="approve">Approve</button><button class="btn-secondary flex-1" name="action" value="reject">Reject</button></div></form></div></div>@endforeach</div><div class="mt-6">{{ $donations->links() }}</div>@endsection
